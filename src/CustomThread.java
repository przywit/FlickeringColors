/*
import java.awt.*;

public class CustomThread extends Thread {
    Block block;
    Graphics2D graphics2D;

    public CustomThread(int x, int y, Color color) {
        block = new Block(x, y, color);
    }

        @Override
    public void run() {
        graphics2D.setColor(block.getColor());
        graphics2D.fillRect(block.getX() * block.getBlockSize(), block.getY() * block.getBlockSize(), block.getBlockSize(), block.getBlockSize());
    }

    public void draw(Graphics2D graphics2D) {
        this.graphics2D = graphics2D;
    }

*/
/*    public void setBlock(Block block) {
        this.block = block;
    }*//*

}
*/
